export default function Footer() {
    return (
        <footer id="disclaimer">
            *Note: the current version of this app only has a limited selection of characters*
        </footer>
    )
}